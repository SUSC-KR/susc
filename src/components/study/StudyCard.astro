---
const { path, studyName, studyImg, studyPeriod, studyType, maxPeople, memberNameList } = Astro.props;

interface Porps {
	studyName: string;
	studyImg: string;
	studyType: string;
	studyPeriod: string[];
	maxPeople: number;
	memberNameList: string[];
}

let currentPeople = memberNameList.length;
let studyThumbnail = studyImg || "favicon.png";

---

<div
	class="group p-6 sm:p-8 rounded-3xl bg-white border border-gray-100 dark:shadow-none dark:border-gray-700 dark:bg-gray-800 bg-opacity-50 shadow-2xl shadow-gray-600/10"
>
	<a href={path}>
		<div class="h-32  w-full flex flex-col justify-center items-center relative overflow-hidden rounded-xl">
			<img
				src={studyThumbnail}
				alt="art cover"
				loading="lazy"
				width="1000"
				height="667"
				class="h-auto w-44 object-cover transition duration-500 group-hover:scale-105"
			/>
		</div>
		<div class="text-center mt-6 relative">
			<h3 class="text-2xl font-semibold text-gray-800 dark:text-white">
				{studyName}
			</h3>
			<p class="mt-6 mb-8 text-gray-600 dark:text-gray-300">
				{currentPeople} / {maxPeople} 명
			</p>
			<div class="flex justify-center mt-4">
				{studyPeriod.map((studyTime: string) => (
					<span class="bg-blue-100 text-blue-800 text-sm font-medium me-2 px-2.5 py-0.5 rounded dark:bg-gray-700 dark:text-blue-400 border border-blue-400 inline-flex items-center justify-center">
						{studyTime}
					</span>
				))}
			</div>
		</div>
	</a>
</div>